# Projects
Python projects that do not belong to any particular personal project

1. StocksandShares_V2.py
   # Allows for any given stock over a given time range to be plotten in a line graph

2. Routing.py
   # Given a destination and starting coordinates, a route via a given transport mode is created and exported as a geojson file
